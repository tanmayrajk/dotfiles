#!/bin/sh

DIR="$HOME/pics/ss"
mkdir -p "$DIR"

FILE="$DIR/$(date +'%Y-%m-%d_%H-%M-%S').png"

GEOM="$(slurp)" || exit 0

grim -g "$GEOM" "$FILE" || exit 0

wl-copy < "$FILE"

notify-send \
	-i "$FILE" \
	"Screenshot taken" \
	"$(basename "$FILE") copied to clipboard"
